var mongoose = require('mongoose');
var passport = rqeuire('passport');

const userCredentials = require('../models/userCredentials')
const productData = require('../models/productData');
const userData = require('../models/userData');



