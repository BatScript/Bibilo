<%- include('header'); -%>
<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="/landing">Home</a>

        <a id="r" href="/logout">logout</a>
</nav>

<% if(total){ %>
    <h1 class="text-center">Hey! you've got a good taste</h1>
<% } else { %>
    <h1 class="text-center">Wow! such desolation</h1>
<% } %>


<div class="container cartlist">

    <table>
        <% for(i = 0; i < s1; i++) { %>
        <tr>
            <td>
                <div class="dataname">
                    <img class="cartbookimage" src="<%= cartBookArray[i].image %>" alt="">
                    <p><%= cartBookArray[i].name %></p>
                </div>
            </td>
            <td>
                <div class="qty">
                    <div class="cartval">
                        <div class="buttoncart">
                            <form class="formtwo" method="post" action="/cart/<%= cartBookArray[i]._id %>">
                                <button name="indec" value="decrement" class="btn btn-primary btn-circle addsub">
                                    <p>-</p>
                                </button>
                                <p><%= cartQty[i].qty %></p>
                                <button name="indec" value="increment" class="btn btn-primary btn-circle addsub">
                                    <p>+</p>
                                </button>
                                <button name="indec" value="del" class="btn btn-danger btn-circle addsub">
                                    <p>X</p>
                                </button>
                            </form>
                        </div>

                        <div class="money">
                            <p>for ₹<%=cartBookArray[i].price * cartQty[i].qty%></p>
                        </div>
                    </div>

                </div>
            </td>
        </tr>
        <% } %>
    </table>
</div>

<% if(total){ %>
    <h1 class="text-center">Total: ₹ <%= total %></h1>
<% } else { %>
    <h1 class="text-center display-1">😔</h1>
<% } %>



<p class="text-center"><a href="/landing">Homepage</a></p>


<%- include('footer'); -%>